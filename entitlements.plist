<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Microphone access, two keys, one for each check macOS performs:

        com.apple.security.device.audio-input
          The Hardened Runtime key. macOS Tahoe 26 checks THIS one when deciding
          whether the app can appear in System Settings, Privacy & Security,
          Microphone. Without it the app never gets a TCC entry.

        com.apple.security.device.microphone
          The App Sandbox flavour, kept as the macOS 14-25 fallback.

      No sandbox key: this app is not sandboxed.
    -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.microphone</key>
    <true/>
</dict>
</plist>
