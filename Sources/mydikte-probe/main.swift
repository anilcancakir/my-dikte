import Foundation

// Stub only: Step 2 fills in the real probe (WAV path, model id, glossary,
// the Groq multipart request, and the timing split). This target exists so
// Package.swift has exactly one owner and Step 2 never touches the manifest.
print("Usage: mydikte-probe <wav-path> <model-id> [--glossary <terms>]")
exit(0)
